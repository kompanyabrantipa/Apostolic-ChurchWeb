# MongoDB Production Configuration for Apostolic Church Louisville
# This file contains production-specific environment variables

# MongoDB Database Configuration (Production)
MONGODB_URI=mongodb+srv://church_admin:SECURE_PASSWORD@apostolic-cluster.mongodb.net/apostolic_church_prod?retryWrites=true&w=majority
MONGODB_USERNAME=church_admin
MONGODB_PASSWORD=SECURE_PASSWORD

# JWT Configuration
JWT_SECRET=apostolic_church_jwt_production_secret_key_2024_very_secure_minimum_32_chars_change_this
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3001
NODE_ENV=production

# Admin User Configuration (for initial setup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,webp,mp4,webm,ogg,avi,mov,mp3,wav,m4a

# CORS Configuration (Production)
FRONTEND_URL=https://apostolicchurchlouisville.org
ALLOWED_ORIGINS=https://apostolicchurchlouisville.org,https://www.apostolicchurchlouisville.org

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Stripe Configuration (Production)
STRIPE_PUBLISHABLE_KEY=pk_live_your_production_publishable_key_here
STRIPE_SECRET_KEY=sk_live_your_production_secret_key_here

# MongoDB-Specific Configuration
MONGODB_MAX_POOL_SIZE=20
MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
MONGODB_SOCKET_TIMEOUT_MS=45000

# Performance Settings
ENABLE_GZIP=true
ENABLE_RESPONSE_CACHING=true
CACHE_TTL=3600

# Security Settings
HELMET_ENABLED=true
CONTENT_SECURITY_POLICY=true
HSTS_MAX_AGE=31536000
REFERRER_POLICY=strict-origin-when-cross-origin
X_FRAME_OPTIONS=DENY
X_CONTENT_TYPE_OPTIONS=nosniff

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# Logging Settings
ENABLE_ACCESS_LOG=true
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# SSL Configuration (if using HTTPS)
SSL_ENABLED=false
SSL_CERT_PATH=/etc/ssl/certs/apostolicchurch.crt
SSL_KEY_PATH=/etc/ssl/private/apostolicchurch.key
